{
  "name": "bridgeaid",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check js/app.js && node --test",
    "start": "python -m http.server 8080"
  }
}
